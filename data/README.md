# Data directories

- `raw/` immutable source snapshots
- `interim/` parsed source-shaped tables
- `processed/` analysis-ready tables
- `external/` optional manually obtained or non-core public files

The seed repo keeps only placeholder `.gitkeep` files and demo-generated CSVs.
